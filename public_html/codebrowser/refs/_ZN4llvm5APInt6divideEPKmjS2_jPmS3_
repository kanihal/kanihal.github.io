<dec f='include/llvm-7/llvm/ADT/APInt.h' l='191' type='static void llvm::APInt::divide(const llvm::APInt::WordType * LHS, unsigned int lhsWords, const llvm::APInt::WordType * RHS, unsigned int rhsWords, llvm::APInt::WordType * Quotient, llvm::APInt::WordType * Remainder)'/>
<doc f='include/llvm-7/llvm/ADT/APInt.h' l='185'>/// An internal division function for dividing APInts.
  ///
  /// This is used by the toString method to divide by the radix. It simply
  /// provides a more convenient form of divide for internal use since KnuthDiv
  /// has specific constraints on its inputs. If those constraints are not met
  /// then it provides a simpler form of divide.</doc>
