<def f='include/llvm-7/llvm/ADT/STLExtras.h' l='318' ll='322' type='void llvm::filter_iterator_base::filter_iterator_base&lt;WrappedIteratorT, PredicateT, IterTag&gt;(WrappedIteratorT Begin, WrappedIteratorT End, PredicateT Pred)'/>
<doc f='include/llvm-7/llvm/ADT/STLExtras.h' l='315'>// Construct the iterator. The begin iterator needs to know where the end
  // is, so that it can properly stop when it gets there. The end iterator only
  // needs the predicate to support bidirectional iteration.</doc>
