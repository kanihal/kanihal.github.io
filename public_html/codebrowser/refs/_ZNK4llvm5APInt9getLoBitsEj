<dec f='include/llvm-7/llvm/ADT/APInt.h' l='584' type='llvm::APInt llvm::APInt::getLoBits(unsigned int numBits) const'/>
<doc f='include/llvm-7/llvm/ADT/APInt.h' l='578'>/// Compute an APInt containing numBits lowbits from this APInt.
  ///
  /// Get an APInt with the same BitWidth as this APInt, just zero mask
  /// the high bits.
  ///
  /// \returns the low &quot;numBits&quot; bits of this APInt.</doc>
